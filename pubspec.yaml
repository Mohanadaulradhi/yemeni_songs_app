name: yemeni_songs_app
description: تطبيق الأغاني اليمنية
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  flutter_bloc: ^8.1.4
  equatable: ^2.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  dio: ^5.4.0
  connectivity_plus: ^5.0.2
  just_audio: ^0.9.36
  cached_network_image: ^3.3.1
  flutter_dotenv: ^5.1.0
  path_provider: ^2.1.2
  go_router: ^13.0.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - .env
